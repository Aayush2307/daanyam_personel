# SQLite for MVP (fast local setup)
DATABASE_URL="file:./dev.db"

# Required for login/auth JWT signing
JWT_SECRET="replace-with-long-random-secret"

# Optional: if absent, real-seva endpoint returns mock orders
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
